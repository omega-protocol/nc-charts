{{- define "crd_clustertriggerbinding-triggers" -}}
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: clustertriggerbindings.triggers.tekton.dev
  labels: {{- include "helpers.labels.labels" (dict "Global" $ "Component" "tekton" "PartOf" "tekton-triggers") | nindent 4 }}
  annotations:
    "helm.sh/hook": "crd-install"
spec:
  group: triggers.tekton.dev
  scope: Cluster
  versions:
    - name: v1alpha1
      served: true
      storage: true
  names:
    kind: ClusterTriggerBinding
    plural: clustertriggerbindings
    singular: clustertriggerbinding
    shortNames:
      - ctb
    categories:
      - tekton
      - tekton-triggers
  subresources:
    status: {}
  version: v1alpha1
...
{{- end -}}
{{- include "helpers.template.overlay" ( dict "Global" $ "template_definition" "crd_clustertriggerbinding-triggers" ) }}
